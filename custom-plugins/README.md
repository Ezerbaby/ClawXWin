# Custom Extensions

本目录存放自定义 OpenClaw 扩展插件，通过 `pnpm run sync:extensions` 脚本从 ClawWin2.0 项目同步。

## 同步命令

```bash
pnpm run sync:extensions
```

## 目录结构

```
custom-plugins/extensions/
├── aliyun-opensearch-plugin/   # 阿里云 OpenSearch 搜索插件
└── ...                         # 其他自定义插件
```
